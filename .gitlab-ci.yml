stages:
  - build

.gradle-image: &gradle-image
  image: gradle:5.5-jdk12

build:
  <<: *gradle-image
  stage: build
  variables:
    CLIENT_KEY: $SPOTIFY_CLIENT_KEY
  script:
    - gradle clean build
  only:
    - master